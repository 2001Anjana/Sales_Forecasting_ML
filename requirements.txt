# Core data stack
pandas>=2.0
numpy>=1.24
openpyxl>=3.1          # read the .xlsx
pyarrow>=14.0          # parquet for interim/processed data

# Visualisation
matplotlib>=3.7
seaborn>=0.12

# Notebooks
jupyter>=1.0
ipykernel>=6.0

# --- needed from step 4 onward (modelling); listed now so the env is complete ---
scikit-learn>=1.3
lightgbm>=4.0
statsmodels>=0.14      # SARIMA / STL decomposition
# prophet>=1.1         # optional comparison model (heavier install)
